# Iris Continuous Training — Python dependencies
# Pin minor versions for reproducibility; allow patch updates with ~=

scikit-learn~=1.4
pandas~=2.1
numpy~=1.26
scipy~=1.11
matplotlib~=3.8
PyYAML~=6.0

# MLflow — experiment tracking + model registry
mlflow~=2.10

# Drift detection
evidently~=0.4.33   # pin to 0.4.x; 0.7+ has breaking API changes

# Prometheus (for optional metric push)
prometheus-client~=0.19

# Utilities
tqdm~=4.66

# Optional dependencies for testing and development
